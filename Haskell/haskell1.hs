[x | x <- [1..10], odd x];
List comprehension:

concadination of lists: ++
Associativity (right):  ':' (also cons operator)

Length (as it says)
Null (checks if list is empty or no)

YOU CAN CREATE INFINITE LISTS XDDDDDDDDDDDDDDDDDDDDDDD
[1..]

you can take the tail of an infinite series
Always use null to test if the set is empty or not cause some haskell fuccboi might just be programming in infinite lists.

map (function) [list]
filter (function) [list]
